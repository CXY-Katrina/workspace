确认：**失败 run 没有发布 Qwen3-32B-QuaRot 的 benchmark results JSON，也没有对应的 ascend-logs artifact**。失败 job 在 benchmark 失败后直接走 assert 失败路径，未生成/上传 perf JSON artifact（只有其他通过的模型有）。good run 有 `main-Qwen3-32B-QuaRot.json`，bad run 没有 → 这是 **Artifact 证据缺口**，但失败日志本身已含完整指标，不阻碍结论。

证据链已完整，达到停止条件。现在输出报告。

---

# CI 失败离线取证分析报告

## 流水线
- https://github.com/vllm-project/vllm-ascend/actions/runs/35376647863/job/105792706494  bad
- https://github.com/vllm-project/vllm-ascend/actions/runs/35410560177/job/105810209528  good

## 一、观察到的失败

- **Workflow**: Nightly-A3
- **Job**: `single-node (main, Qwen3-32B-QuaRot, linux-aarch64-nightly-a3-2, Qwen3-32B-QuaRot-eagle3.yaml) / Qwen3-32B-QuaRot`
- **失败步骤**: Step #15 `Run Pytest (YAML-driven)`
- **失败测试**: `test_single_node[Qwen3-32B-QuaRot]`，断言于 `tools/aisbench.py:342`：`AssertionError: some aisbench cases failed`
- **直接失败原因**：**性能阈值未达标**。aisbench 性能验证失败：
  - 实测 `Output Token Throughput = 377.0297 token/s`
  - 阈值 `0.97 × baseline 392 = 380.24 token/s`
  - 低于阈值约 3.2 token/s（约 0.84%）
- **失败 case**：`{'case_type': 'performance', 'dataset_path': 'vllm-ascend/GSM8K-in3500-bs400', 'num_prompts': 80, 'max_out_len': 1500, 'batch_size': 20, 'baseline': 392, 'threshold': 0.97}`

非崩溃、非超时、非基础设施错误。80/80 请求全部成功（Failed Requests=0），生成 120000 tokens，Benchmark Duration 318277ms。

## 二、已确认事实

1. **被测配置**：`vllm serve vllm-ascend/Qwen3-32B-W8A8-QuaRot --enforce-eager --no-enable-prefix-caching --tensor-parallel-size 2 --max-model-len 8192 --trust-remote-code --distributed-executor-backend mp --gpu-memory-utilization 0.9 --speculative-config {"method": "eagle3", "model": "RedHatAI/Qwen3-32B-speculator.eagle3", "num_speculative_tokens": 3}`。eagle3 投机解码，TP=2，W8A8 QuaRot，enforce_eager，A3 (Ascend910)。

2. **good_commit / bad_commit 边界（已从两侧 Job checkout 日志核实）**：
   - good_commit = `12be3d34a1510be8e19542f577e732a12c47a1d1`（Run #15728, 09-17）
   - bad_commit = `c8addbb24fe8fa820bb79685c116e43ba89b77e9`（Run #35376647863, 09-18）
   - 两侧均为主分支 `main`，同名 Job，同硬件 A3，分支兼容。

3. **last-good 对照（性能指标）**：

| 指标 | GOOD (12be3d34) | BAD (c8addbb2) | 变化 |
|---|---|---|---|
| Output Token Throughput | 400.238 token/s | 377.0297 token/s | **-5.8%** |
| Benchmark Duration | 299821 ms | 318277 ms | +6.2% |
| Prefill Token Throughput | 2136.31 | 2107.02 | -1.4% |
| Total Token Throughput | 1376.66 | 1296.83 | -5.8% |
| baseline (YAML) | 409.4417 | 392 | 已下调 |
| pass_fail | pass | **fail** | — |

4. **稳态 decode 吞吐系统性下降**：对两端 Running:20 稳态段的 `Avg generation throughput` 采样序列逐点对比，BAD 全程系统性低于 GOOD 约 6-7%（GOOD 均值约 437.9 token/s，BAD 均值约 406.6 token/s），贯穿整个稳态区域，非单点/尾部噪声。

5. **eagle3 acceptance 两端一致（关键反证）**：
   - 稳态 Mean acceptance length：GOOD ≈ 2.47，BAD ≈ 2.47
   - 收敛段：GOOD 3.91，BAD 3.88
   - Avg Draft acceptance rate：GOOD 48.9%，BAD 49.0%
   - **投机解码接受率未退化**，说明吞吐下降不来自 acceptance 退化，而来自单次 forward 执行时间变长。

6. **环境层 good/bad 完全一致**：
   - 镜像 `nightly-ci-main-a3`，CANN `9.1.0_20260731131545`（相同），npu-smi 25.5.2（相同），vLLM v0.28.0，Python 3.12.13，Ascend910，serve_cmd / spec config 完全一致。
   - 本 Job step #5/#6/#8（Checkout/Move/Install vllm-ascend）均 skipped，使用预构建 nightly 镜像（`skip_build_image: true`）。

## 三、回归边界

- vllm-ascend 提交区间：`12be3d34..c8addbb2`（共约 90+ 提交）。
- 区间内与本测试路径（eagle3 spec decode / dense decode / W8A8 quant）相关的候选已逐一核查。

## 四、主要嫌疑 / 候选假设（均未达归因标准）

### 候选 A：#16678 `[Revert] Revert "[Performance][Spec Decode] Avoid cloning unused raw target logits"` — **已排除（rejected）**
- **支持证据**：该提交作用于 `vllm_ascend/sample/rejection_sampler.py` 的 `AscendRejectionSampler`，本测试走 eagle3 spec decode 路径；方向上"恢复 clone"与吞吐下降一致。
- **强反证（决定性）**：经调用链与时序核实，good_commit (`12be3d34`) 时该文件的 clone 条件已为 `if not self.is_processed_logprobs_mode: target_logits = target_logits.clone()`，与 #16678 revert 后的 bad_commit 状态**完全相同**。原优化 #16211（merge `0526083dd4`）**不在 good_commit 历史中**（`git merge-base --is-ancestor` 确认）。即 good 和 bad 两次运行都是"clone"行为，#16678 未改变本测试的 rejection sampler 行为。**不构成 good→bad 回归源**。

### 候选 B：#16328 `[Performance][Attention] Fuse SFA K-path ops and enable PROLOG_V3 fused decode` — **降级为低优先候选（非归因）**
- 该提交仅触及 `attention/sfa_v1.py`、`attention/indexer.py`、`device/device_op.py` 及对应 UT，明确针对 **GLM5.2 SFA (Sparse Flash Attention) / quantized SFA layers / MLA prolog** 路径。Qwen3-32B 为 dense 模型，不走 SFA/MLA prolog 路径。`device_op.py` 共享部分改动尚未完全排除调用链，但无证据指向 Qwen3 dense decode。属标签级降低优先级，未达 rejected 强反证，亦未达归因。

### 候选 C：区间内其他性能提交（#16624 scatter_nd、#16245 MoE foreach_add、#15590 GDN conv、#15970 DCP decode、#16489 MRV2 MTP 等）
- 均针对 MoE / GDN / DCP / MLA / MRV2 / 310P 等非本测试路径，Qwen3-32B dense eagle3 不涉及。未发现能闭合"dense decode -6%"的提交。

## 五、支持主要结论的证据

- 失败的直接原因是吞吐阈值未达标（377.03 < 380.24），日志与断言明确。
- 吞吐劣化是真实且系统性的（稳态逐点 -6~7%，全程 OTT -5.8%），非噪声级波动。
- good/bad 环境层（CANN/npu-smi/硬件/配置）一致，排除了显式的环境/配置差异。
- acceptance 一致，排除了 spec decode 正确性退化。

## 六、反证和证据缺口

1. **Artifact 证据缺口**：失败 Job 未发布 `main-Qwen3-32B-QuaRot.json` perf artifact，也无对应 ascend-logs（失败后直接 assert 退出）。无法做 kernel 级 ascend profiling 对照。失败日志自身已含完整指标，不阻碍直接原因结论，但阻碍更深归因。
2. **镜像 digest 证据缺口**：`skip_build_image: true` + `base_image_date: auto`，09-17 与 09-18 拉取的 `nightly-ci-main-a3` 标签可能指向不同 image digest（nightly 镜像每日重建，bake 入当天 vllm-ascend 及可能的 vLLM 上游）。日志未记录 image digest / vLLM 上游精确 commit，**无法隔离 vLLM 上游或镜像构建层的潜在变更**。
3. **量级合理性缺口**：吞吐下降 ~6%，但区间内未发现能解释该量级的单一 vllm-ascend 致错提交；既不能确认代码回归，也不能完全排除 runner/硬件微观波动或镜像层上游变更。

## 七、已排除假设

- **基础设施/网络/磁盘/权限/超时**：无相关错误，80/80 请求成功，job 在 792s 内正常完成。
- **CANN/驱动版本变化**：两侧 CANN `9.1.0_20260731131545`、npu-smi 25.5.2 完全相同。
- **测试配置/serve_cmd 变化**：两侧完全一致。
- **eagle3 acceptance 退化**：acceptance length 与 draft acceptance rate 两端一致。
- **#16678 logits clone 构成回归**：good/bad 行为相同，强反证排除。
- **baseline 上调导致失败**：baseline 实际从 409.4417 **下调**到 392（PR #16815），方向是放宽阈值；失败源于实测进一步劣化到 377，而非门槛抬高。

## 八、关于 baseline 调整的说明

PR #16815（`47a66076d1`，区间内）将本测试 baseline 从 `409.4417` 下调到 `392`，commit message 说明"to match the latest test results"——即提交者已观察到吞吐从 ~400 区间下滑至 ~392 区间，故下调基线适配。本次失败实测 377.03，**低于下调后的门槛 380.24**。这表明：baseline 下调本身不是致错原因（方向相反），反而印证了**近期存在持续的吞吐劣化趋势**，且本次劣化幅度已超出下调后的容忍范围。**不建议进一步下调 baseline 来消除本次失败**——在未完成同配置复测、未定位真实回归源前，下调基线只会掩盖问题。

## 九、建议的后续动作

1. **同配置复测**（首要）：在 A3 同 runner、同镜像、同 commit `c8addbb2` 上重跑 `Qwen3-32B-QuaRot-eagle3.yaml` 2-3 次，确认 377 token/s 是否稳定复现。若复测回到 392+，则属 runner/硬件波动；若稳定 <380，则确认真实回归。
2. **固定并记录镜像 digest / vLLM 上游 commit**：在 nightly workflow 中输出 image digest 与 vLLM 上游 SHA，以便隔离 vLLM 上游或镜像构建层的变更。
3. **若复测确认回归**：对区间内 dense decode / W8A8 quant / eagle3 proposer 路径做 bisect（重点排查 #16328 的 `device_op.py` 共享改动是否波及 dense，以及 vLLM 上游 09-17→09-18 的 decode 路径变更）。
4. **基线保持不变**：在上述复测与归因完成前，不调整 baseline。
5. **补全失败 Job 的 perf artifact 上传**：使失败 case 也能上传指标 JSON 与 ascend-logs，便于后续 kernel 级对照。

## 十、关联 PR

**未归因到 PR**。离线证据确认了吞吐劣化现象与 last-good 边界，但在 vllm-ascend `12be3d34..c8addbb2` 区间内未能闭合"失败日志 → 运行路径 → 受影响源码 → 具体改动"的完整因果链至单一 PR；镜像层 vLLM 上游变更无法离线隔离。按停止规则，关联 PR 留空，不猜测候选。

## 结论等级

**likely（针对直接失败原因）**：失败日志、断言、性能指标、last-good/bad 对照、环境一致性、acceptance 反证均已形成一致证据链，离线取证充分确认"吞吐阈值未达标"的直接原因；**待运行复现**以区分真实代码回归与 runner/镜像层波动。

**insufficient（针对 PR 归因）**：缺少镜像 digest / vLLM 上游精确版本、缺失失败侧 perf artifact、区间内无单一提交能量级匹配，无法将 ~6% 吞吐劣化闭合到具体 PR。

---

```json
{
  "problem_category": "测试用例",
  "root_cause_summary": "Qwen3-32B-QuaRot eagle3 性能验证失败：实测 Output Token Throughput 377.03 token/s 低于阈值 0.97×392=380.24。last-good(12be3d34, OTT=400.24)→bad(c8addbb2, OTT=377.03) 系统性下降约5.8%，贯穿稳态全程，eagle3 acceptance 两端一致排除投机解码退化，CANN/驱动/配置两端一致。直接失败原因为吞吐劣化未达阈值，离线取证充分，待运行复现以区分真实代码回归与runner/镜像层波动。区间内未闭合到单一PR（#16678经时序核实good/bad行为相同已排除，#16328仅触及SFA/GLM5路径不作用于Qwen3 dense）。",
  "improvement_measures_summary": "1) A3同runner/同镜像/同commit复测2-3次确认377是否稳定复现；2) nightly workflow记录镜像digest与vLLM上游SHA以隔离上游/镜像层变更；3) 复测确认回归后对dense decode/W8A8 quant/eagle3路径bisect(含#16328 device_op共享改动与vLLM上游09-17→09-18 decode变更);4) 归因完成前baseline保持不变,不建议下调;5) 补全失败Job的perf artifact与ascend-logs上传以便kernel级对照。"
}
```
