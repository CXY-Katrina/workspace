# Qwen3-32B-QuaRot A3 性能调查：历史复测、CI 对比与 CPU 绑核分析

调查日期：2026-09-19。本文承接 [原始分析 32B-A3.md](32B-A3.md)，汇总后续八轮正式性能测试和离线代码回放；与原报告不一致之处，以本文明确区分的对照组及证据为准。

## 1. 结论与证据边界

1. **已经确定隔离后 CPU 绑定改变的原因。** A3 始终使用 `global_slice`，`enable_cpu_binding` 默认仍为 `True`。容器仅暴露物理芯片 10/11 后，它们重编号为逻辑 0/1，可见逻辑设备数从 16 降到 2，可用 CPU 却仍为 0–639。同一算法因此从每卡 40 核变为每卡 320 核；内存迁移目标由 CPU 池起点推导，随之从 NUMA5/5 变为 NUMA0/4。硬件 NUMA 拓扑没有改变。
2. **换到模块 5 本身没有复现下降。** 全设备可见时，模块 0 三轮平均 396.5194、模块 5 两轮平均 397.2979 token/s。
3. **默认自动绑核下，隔离模块 5 的单轮出现下降。** 吞吐 379.1176，较同模块非隔离均值低 4.5760%，低于 380.24 门槛。但 CPU/NUMA、容器权限及 IRQ 行为同时存在差异，不能将下降唯一归因于设备隔离或某个绑核因素。
4. **固定 CPU/NUMA 后，没有复现隔离导致下降。** 两组均显式关闭自动绑核，固定 CPU400–479、NUMA5：全设备可见为 377.4380，仅模块 5 可见为 388.1388 token/s。隔离组高 2.8351%。每组仅一次，不能据此认定隔离必然提速或完全排除环境波动。
5. **尚未定位引入性能回归的 PR。** 已定位绑定变化的软件机制，不等于已证明其解释全部吞吐差异。用户指定的新 good/bad 推理代码相同，但物理设备、CPU 布局和构建过程不同，不能宣称环境完全一致。

最后一阶段仅做源码分析和离线回放，没有再次运行完整性能测试，也没有修改推理源码。

## 2. 失败用例与基线

- Job：`single-node (main, Qwen3-32B-QuaRot, linux-aarch64-nightly-a3-2, Qwen3-32B-QuaRot-eagle3.yaml) / Qwen3-32B-QuaRot`。
- 主模型：`vllm-ascend/Qwen3-32B-W8A8-QuaRot`；draft：`RedHatAI/Qwen3-32B-speculator.eagle3`。
- 用户已说明 reshape and cache 替换为 nd scatter update 存在已知性能影响，基线已下调到 392。本次调查使用该基线，没有重新验证该算子替换的因果关系，也未继续下调基线。
- 判定门槛：`392 × 0.97 = 380.24 token/s`。
- bad CI 实测 377.0297，低于门槛 3.2103 token/s，约 0.84%；80/80 请求成功，属于性能断言失败，不是请求错误或服务崩溃。

## 3. 服务器固定环境与测试方法

### 3.1 安装版本

| 项目 | 服务器实测环境 |
| --- | --- |
| vLLM Ascend | `c8addbb24fe8fa820bb79685c116e43ba89b77e9`，调查开始时核实的 main |
| vLLM | release `v0.28.0`，`2cf0a6915ce544dc493a0990f2ea38d81601128a`；依据上述 Ascend 的 Dockerfile.a3 pin |
| 原始镜像 | `7b1a4d2ec0dedbec7592e208dc0a821882eb93a6f654a7717ba368b54d062891` |
| 后续安装快照 | `af29cefa14bc918c83d9b644a85d2763f57804f0466a4e196b0e2dfc592b20e1`，由原测试容器本机保存 |
| CANN | 9.1.0，内部版本 `V100R001C25B114` |
| 驱动 / npu-smi | 25.5.0 |
| torch / torch_npu | `2.10.0+cpu` / `2.10.0.post4` |
| AISBench | `v3.1-20260609-master`，`0da56eadb2ac85c31c2540f4f5b69af3ec5717a5` |
| 设备 | A3；8 个物理模块、16 个逻辑芯片；模块 5 对应物理芯片 10/11 |

复用原镜像并安装固定源码，后续使用本机快照保留相同安装产物，没有下载替代镜像。Ascend 自定义算子沿用从同一 c8 提交、同一 B114 环境完整编译的产物。旧 dist-info 曾导致早期安装日志显示旧版本串，已备份并移出；实际源码路径及 Git SHA 始终为上述版本，最终包版本为 `0.19.1rc2.dev2256+gc8addbb24`。

### 3.2 固定负载与执行流程

- TP=2，eager，关闭 prefix cache，max_model_len=8192，Eagle3 `num_speculative_tokens=3`。
- `distributed_executor_backend=mp`，`gpu_memory_utilization=0.9`。
- 数据集 `vllm-ascend/GSM8K-in3500-bs400`；80 请求、并发上限 20、每请求输出 1500 tokens、request_rate=0。
- `temperature=0`、`ignore_eos=True`。每轮实际输入 292753、输出 120000 tokens，80/80 成功。
- 每轮独立启动服务，执行默认 completion 检查，再调用仓库 `AisbenchRunner` 执行 AISBench；结束后停止该轮服务。使用辅助启动脚本，并非直接执行完整 pytest。

**completion 与预热的区别：** `San Francisco is a`、max_tokens=10 是单节点框架默认的功能检查，配置来源为 `single_node_config.py`，`test_single_node.py` 在 benchmark 前执行并检查非空。辅助脚本使用单字符串 prompt，原框架使用单元素列表，均核验非空结果。它不应被称为 AISBench 性能预热。

AISBench 固定版本自身默认 `num_warmups=1`，本次日志均为预热请求 1、成功 1。日志中的 concurrency=20 表示并发上限，不代表有 20 个预热请求。每轮重启服务是本次复测的控制方式，不是仓库要求“必须重启三次”。启动、completion 和预热时间不计入正式吞吐。

## 4. 八轮正式性能结果

以下阈值均为 380.24 token/s。“True”组保持默认自动绑核；“False”组为单独的固定 CPU/NUMA 诊断实验，不能与前六轮直接比较并归因。

| 标签 | 容器可见设备 / 实际使用 | 自动绑核 | 输出吞吐 token/s | 判定 |
| --- | --- | --- | --- | --- |
| main-release-1 | 全部 / 模块0芯片0、1 | True | 398.3011 | 通过 |
| main-release-2 | 全部 / 模块0芯片0、1 | True | 397.9307 | 通过 |
| main-release-3 | 全部 / 模块0芯片0、1 | True | 393.3263 | 通过 |
| npu1011-1 | 全部 / 模块5芯片10、11 | True | 398.0245 | 通过 |
| npu1011-2 | 全部 / 模块5芯片10、11 | True | 396.5713 | 通过 |
| isolated-module5-1 | 仅模块5芯片10、11 / 同左 | True | 379.1176 | 未通过 |
| matched-full-1 | 全部 / 模块5芯片10、11 | False，固定CPU/NUMA | 377.4380 | 未通过 |
| matched-isolated-1 | 仅模块5芯片10、11 / 同左 | False，固定CPU/NUMA | 388.1388 | 通过 |

性能未通过的轮次也均为 80/80 请求成功；测试脚本 exit=1 表示吞吐未达门槛。

### 4.1 三次复测与换模块

- 模块 0 三轮均值 **396.5194**，范围 393.3263–398.3011；极差/均值 **1.2546%**，样本标准差 2.7715，样本 CV **0.6989%**。耗时分别为 301.2796、301.5600、305.0902 秒。
- 模块 5 两轮均值 **397.2979**，比模块 0 均值高 **0.1963%**；极差/均值 0.3658%。耗时分别为 301.4890、302.5937 秒。按用户要求第二轮结束后停止，未运行第三轮。
- 模块 0 的 main CPU 为 2–37、42–77，内存迁移目标为 NUMA0/0；模块 5 的 main CPU 为 402–437、442–477，目标为 NUMA5/5。
- 两次模块 5 结果均落在模块 0 三次结果范围内，不支持“模块 5 本身必然明显更慢”。样本较少，不能据此断言模块间绝无差异。
- 先前探索性结果 `bad-b114-2=395.3697` 不计入正式分组统计。讨论过的逻辑芯片 4/5 未启动测试。

### 4.2 默认自动绑核下，仅隔离模块 5

使用 Ascend runtime、`ASCEND_VISIBLE_DEVICES=10,11`，容器内 `npu-smi info` 仅列模块 5 的物理 10/11；它们在 `npu-smi info -m` 中变为逻辑 0/1，服务使用 `ASCEND_RT_VISIBLE_DEVICES=0,1`。可用 CPU 仍为 0–639。

该容器非 privileged，使用 `--cap-add=ALL --security-opt label=disable --security-opt seccomp=unconfined`、host network、16 GiB shm；原全设备容器为 privileged。因此这轮不是仅改变设备可见性的严格单变量对照。

单轮吞吐 **379.1176**，耗时 **316.5244 秒**，比同模块非隔离均值低 **4.5760%**。TTFT 2178.9 ms、TPOT 49.4 ms、ITL 126.2 ms；非隔离模块 5 的 TPOT 为 47.0/47.2 ms、ITL 为 120.1/120.6 ms，变化主要体现在解码相关指标。

初次隔离访问曾返回 EBUSY/-8020。检查设备 namespace 后发现已有空闲容器持有映射；按用户授权停止空闲占用容器后恢复。未修改 device-share 开关。该过程不计入正式性能统计。

宿主机 irqbalance 为 active。非隔离日志记录 IRQ4919/4920→CPU400/401、IRQ5179/5180→CPU440/441，隔离期间实际读取则为 CPU290–291、286–287、268–269、284–285。启动日志不能保证 IRQ 长期保持原设置；缺少前几轮全过程 IRQ 采样，无法分离其性能贡献。本次没有停止宿主机 irqbalance。

### 4.3 固定 CPU/NUMA，单独对照设备可见性

两组均从相同安装快照启动，Docker HostConfig 全字段一致，环境变量差异仅为设备可见范围和对应逻辑编号：

```text
两组共同设置：
  --cpuset-cpus=400-479 --cpuset-mems=5
  numactl --membind=5 <原 vllm serve 命令>
  --additional-config '{"enable_cpu_binding": false}'

全设备组：ASCEND_VISIBLE_DEVICES=0,1,...,15；ASCEND_RT_VISIBLE_DEVICES=10,11
隔离组：  ASCEND_VISIBLE_DEVICES=10,11；ASCEND_RT_VISIBLE_DEVICES=0,1
```

关闭自动绑核是此诊断对照显式传入的参数，**源码默认值仍为 True，前六轮没有关闭**。目的是避免两组因自动分配算法而使用不同 CPU/NUMA。

| 指标 | 全设备可见 | 仅模块5可见 |
| --- | --- | --- |
| 输出吞吐 token/s | 377.4380 | 388.1388 |
| 平均 TTFT ms | 2188.5 | 2179.9 |
| 平均 TPOT ms | 49.6 | 48.2 |
| 平均 ITL ms | 126.8 | 123.1 |
| 请求成功 / 失败 | 80 / 0 | 80 / 0 |

隔离组高 **2.8351%**。已核验相同镜像、版本、服务命令、负载，以及所有采样 API/Engine/Worker 线程的 CPU affinity=400–479；内存映射策略主体为 bind:5，另有 local 特殊映射。两组稳态 IRQ 亲和性采样相同。

该结果没有复现“隔离本身固定损失约 4.6%”。但仅各一次、顺序为先全设备后隔离，不能排除时序波动，不能证明自动绑核是唯一原因，也不能把与前六轮不同的线程分配策略混合统计。

## 5. 用户指定的两次 GitHub Actions 对照

- [bad：run 35376647863 / job 105792706494](https://github.com/vllm-project/vllm-ascend/actions/runs/35376647863/job/105792706494)
- [good：run 35410560177 / job 105810209528](https://github.com/vllm-project/vllm-ascend/actions/runs/35410560177/job/105810209528)

### 5.1 性能与环境

| 项目 | bad | good |
| --- | --- | --- |
| 输出吞吐 token/s | 377.0297 | 392.1712 |
| Benchmark Duration 秒 | 318.2774 | 305.9888 |
| 成功 / 失败请求 | 80 / 0 | 80 / 0 |
| 输入 / 输出 tokens | 292753 / 120000 | 292753 / 120000 |
| 并发上限 / 平均并发 | 20 / 18.5470 | 20 / 18.4856 |
| 平均 TTFT ms | 1736.8 | 1721.0 |
| 平均 TPOT ms | 48.1 | 46.0 |
| 平均 / 中位 ITL ms | 122.3 / 114.8 | 117.0 / 109.2 |
| vLLM Ascend | c8addbb24 | 691b8264 |
| vLLM | v0.28.0 / 2cf0a691 | 相同 |
| CANN 内部版本 | V100R001C11SPC001B243 | 相同 |
| 驱动 | 25.5.2 | 相同 |
| CPU 架构 | aarch64 | aarch64 |
| 物理模块 / 芯片 | 模块5 / 10、11 | 模块0 / 0、1 |
| PCI 地址 | 89:00.0、8B:00.0 | 9D:00.0、9F:00.0 |

bad 比 good 低约 **3.861%**；TPOT 高约 4.57%，ITL 高约 4.53%，TTFT 仅高约 0.92%。并发及 token 总量相同，不支持负载变小或接受率显著提高解释全部差异。

good 的 `691b8264ac9030483e148c79c8f0e3d9a2f7b82c` 是 bad 的 c8 直接子提交，唯一源码差异为 `tools/rtd_build.sh` 一行空行改成 `#`，推理代码、测试脚本和 YAML 相同。参见 [源码差异](https://github.com/vllm-project/vllm-ascend/compare/c8addbb24fe8fa820bb79685c116e43ba89b77e9...691b8264ac9030483e148c79c8f0e3d9a2f7b82c)。

### 5.2 CPU 布局与构建过程并不相同

| 角色 | bad | good |
| --- | --- | --- |
| 逻辑 NPU0 main | 2–157 | 34–173 |
| 逻辑 NPU0 ACL / release | 158 / 159 | 174 / 175 |
| 逻辑 NPU1 main | 162–317 | 178–317 |
| 逻辑 NPU1 ACL / release | 318 / 319 | 318 / 319 |
| 内存迁移目标 | NUMA0/4 | NUMA0/4 |

两者均开启自动绑核、使用 global_slice，但最终核号不同。相同开关表示执行相同算法，并不保证分配到相同 CPU。两边都是 ARM，不能解释为 ARM/x86 切换。CPU 繁忙程度或 CPU quota 本身不会改变 Cpus_allowed_list 切片结果；核号差异应追查 CPU 允许列表和设备映射。日志未完整记录初始 cpuset、quota 和邻居任务，无法确认独占性。

两边均提示 `/proc/irq is not writable`，未由服务统一设置 IRQ。bad 使用镜像预装 Ascend；good 的 request_id 非空，重新检出源码并构建安装，约耗时 13 分 29 秒，再恢复同版 AISBench。源码相同不能证明自定义算子二进制逐字节相同，日志缺少产物 hash；也不能反向断言重新构建就是提速原因。

两个 runner 实例名不同，但这不足以证明物理宿主机不同；相同 runner 标签也不保证资源配置相同。

周期日志中 Running=20、prompt throughput=0 的窗口显示 good 的 draft 速度更高，而加权 acceptance length 约为 bad 2.52、good 2.49；不支持“仅接受率提高”解释全部差异。该采样不是严格配对的完整迭代剖析。

### 5.3 对原报告的修正：两组 good 不能混用

原报告正文使用旧 good `12be3d34`（约 400.238 token/s）作为回退边界，顶部又列出新 good job `105810209528`。它们是不同对照。

- **旧 good 12be3d34 对 bad c8：** 后续日志核验显示 CANN 内部版本为 B114 对 B243；仅看 CANN 9.1.0 展示版本不能认定环境相同。两者 vLLM 精确提交均为 2cf0a691。
- **新 good 691b8264 对 bad c8：** 均为 B243、驱动 25.5.2，推理源码相同，但物理模块、CPU 布局和构建安装过程不同。
- **本机八轮：** 均在 B114、驱动 25.5.0 下完成，不能据此直接证明 B243 CI 的唯一根因。

因此原报告“环境完全一致”“已排除 CANN/环境差异”等笼统结论不再成立。原报告中的候选 PR 分析也不应被当作本轮已完成的逐个回退验证。

## 6. 隔离后绑核改变的代码原因

以下代码位置均对应测试提交 c8，而不是调查期间本地工作区后续变化。

1. [hardware_profile.py](https://github.com/vllm-project/vllm-ascend/blob/c8addbb24fe8fa820bb79685c116e43ba89b77e9/vllm_ascend/device/hardware_profile.py#L178) 将 A3 的模式设为 GLOBAL_SLICE，两轮日志均未切换模式。
2. [DeviceInfo](https://github.com/vllm-project/vllm-ascend/blob/c8addbb24fe8fa820bb79685c116e43ba89b77e9/vllm_ascend/cpu_binding.py#L106) 解析容器内 `npu-smi info -m`，保存 Chip Logic ID，没有保存 Chip Phy-ID。隔离后仍为物理模块5、芯片10/11，但解析出的逻辑编号为0/1、总数为2。
3. [parse_allowed_cpus](https://github.com/vllm-project/vllm-ascend/blob/c8addbb24fe8fa820bb79685c116e43ba89b77e9/vllm_ascend/cpu_binding.py#L180) 从 `/proc/self/status` 读取 Cpus_allowed_list。本例隔离前后均为0–639，NPU 隔离没有自动收缩 CPU 集合。
4. [build_global_slice_cpu_pool](https://github.com/vllm-project/vllm-ascend/blob/c8addbb24fe8fa820bb79685c116e43ba89b77e9/vllm_ascend/cpu_binding.py#L442) 将全部可用 CPU 按逻辑设备总数均分，再按逻辑编号取切片。本模式不使用 npu-smi topo affinity；分母不是 TP=2，也不是服务实际使用的芯片数。
5. 角色分配预留池前两个 CPU 给 IRQ、末两个给 ACL/release，中间给 main。
6. [bind_memory](https://github.com/vllm-project/vllm-ascend/blob/c8addbb24fe8fa820bb79685c116e43ba89b77e9/vllm_ascend/cpu_binding.py#L639) 使用池首个 CPU 的 NUMA 节点作为 migratepages 目标，而不是查询物理 NPU 的 NUMA 位置。

| 项目 | 全设备可见，服务使用10/11 | 仅10/11可见，服务逻辑0/1 |
| --- | --- | --- |
| 设备总数 | 16 | 2 |
| 每池核数 | 640/16=40 | 640/2=320 |
| CPU 池 | 400–439、440–479 | 0–319、320–639 |
| main | 402–437、442–477 | 2–317、322–637 |
| ACL | 438、478 | 318、638 |
| release | 439、479 | 319、639 |
| 池首个 CPU | 400、440 | 0、320 |
| 内存迁移目标 | NUMA5、5 | NUMA0、4 |

**已完成最小回放：** `replay_binding.py` 从 c8 源码提取实际映射解析、分池、角色分配和内存迁移函数，以已保存的两份设备映射、宿主机 lscpu 为输入，只替换外部命令为记录器。离线执行后逐项断言上述结果与真实 server.log 完全一致，输出 `REPRODUCED`。没有启动服务、加载模型或实际迁移内存。

这里的 NUMA0/4 是迁移命令的目标，不能表述为硬件归属改变或全部内存长期驻留已获证明。migratepages 是一次迁移，不等同于持续设置 numactl --membind。

### 6.1 软件假设为何失效

global_slice 原意是让共享 CPU 集合的进程/DP 组按全局设备编号得到互不重叠的 CPU 池。但“全局”编号实际取自容器内设备视图。当设备 namespace 缩小并重编号、CPU 集合仍是整机范围时，原来的设备与 CPU 对齐关系失效。

本机隔离轮与 bad CI 的逻辑编号0/1、global_slice 和 NUMA0/4 目标一致；具体核号不同，因为本机每池320核，bad CI 每池160核。这能解释绑定机制对应，不能证明性能环境完全复刻。

### 6.2 历史来源及修复方向

Git 历史显示 [PR #6945](https://github.com/vllm-project/vllm-ascend/pull/6945)、提交 `b771ca9a479af384a0542f1bd9ac64d0c03dd014`（2026-03-03）引入 global slicing，当时已有按逻辑设备总数切分以及按 CPU 池起点选择 NUMA 的实现。[PR #15517](https://github.com/vllm-project/vllm-ascend/pull/15517) 修正 worker 选择设备的映射，未修改该切片公式。**这是机制来源，不是本次性能回归 PR 的归因结果。**

- 配置规避：仅10/11可见时同步配置 CPU400–479，可让现有公式分出400–439、440–479。若仍有16个设备可见，仅设置80核不会得到同样结果，因为分母仍为16。
- 源码方向：利用实际设备映射和拓扑建立 NUMA 本地 CPU 池，与容器允许 CPU 求交，并处理同 NUMA 多芯片及多容器重叠。不能只把逻辑0/1替换成物理10/11，却保留 total_npus=2。
- 本轮没有实施源码修复，也没有为验证该方向追加完整性能测试。

## 7. 用例及测试框架历史

| 日期 | PR | 对本用例的影响 |
| --- | --- | --- |
| 2026-05-22 | [#9362](https://github.com/vllm-project/vllm-ascend/pull/9362) | 创建32B YAML：TP2、Eagle3三步、eager、prefix cache关闭、80请求、并发20、输出1500；初始max_model_len=4096、baseline=1、threshold=0.97 |
| 2026-06-08 | [#10100](https://github.com/vllm-project/vllm-ascend/pull/10100) | 删除 --enforce-eager 后错误的独立 True 参数 |
| 2026-06-15 | [#10485](https://github.com/vllm-project/vllm-ascend/pull/10485) | max_model_len改为8192 |
| 2026-08-19 | [#14553](https://github.com/vllm-project/vllm-ascend/pull/14553) | baseline改为409.4417 |
| 2026-09-18 | [#16815](https://github.com/vllm-project/vllm-ascend/pull/16815) | baseline降为392，其他用例参数不变 |

`git log --follow` 可能继续追溯到复制来源 Qwen3-30B YAML；上表按32B文件实际创建边界记录，未将30B历史混入。

- [#10219](https://github.com/vllm-project/vllm-ascend/pull/10219) 于2026-06-09将AISBench pin改为v3.1-20260609-master，至被测版本未再次修改。
- [#14667](https://github.com/vllm-project/vllm-ascend/pull/14667) 增加投机接受率检查；本用例没有配置该检查，仍使用默认completion和performance。
- [#15475](https://github.com/vllm-project/vllm-ascend/pull/15475) 增加spec_decode类型，本用例仍为performance。
- [#16626](https://github.com/vllm-project/vllm-ascend/pull/16626) 加入可选reasoning_effort，随后 [#16832](https://github.com/vllm-project/vllm-ascend/pull/16832) 撤回；本用例未设置该参数。
- 旧边界12be3d34..c8中，tools/aisbench.py、test_single_node.py、single_node_config.py及单节点工作流两端相同；本YAML仅基线下调。conftest.py差异位于本单节点非PD用例不执行的RemotePDServer分支。

现有历史证据不支持“压测负载或吞吐判定算法改变”解释本次差异。基线下调放宽门槛，本身不会让测得吞吐降低。

## 8. 证据归档、清理和未决项

本机调查目录：`artifacts/quarot-main-release-20260919/`；测试服务器归档目录：`/home/cxy/quarot-pr-investigation-20260919/`。以下是已保存的证据索引，不是本仓库附件链接：

| 证据 | 内容 |
| --- | --- |
| main-release-evidence.tar.gz | 模块0三轮manifest、服务日志、功能检查及性能原始输出 |
| npu1011-evidence.tar.gz | 模块5非隔离两轮证据 |
| isolated-module5-evidence.tar.gz | 模块5隔离默认绑核单轮证据 |
| matched-evidence.tar.gz | 固定CPU/NUMA两轮证据、Docker配置、线程亲和性及IRQ采样 |
| summary.json、all-runs-audit.json | 前六轮统计及核验 |
| matched-summary.json | 最后两轮配置一致性核验及结果 |
| ci-comparison.json、compare_ci_logs.py | 两次目标CI日志的提取结果及程序 |
| replay_binding.py、binding-replay.json | c8实际分配函数离线回放及结果 |

八轮测试已完成，测试服务及测试容器已停止，模块5无残留计算进程。为解除空闲设备namespace占用而停止的已有容器保持停止；没有修改宿主机irqbalance或device-share设置。本提交仅包含本文，不上传原始日志、模型、凭据或服务器访问配置。

仍未确定：CPU/NUMA变化对吞吐的独立贡献、IRQ/调度干扰的贡献、CI构建产物是否一致，以及最终是否存在需归因到某一PR的性能回归。后续若继续，应先明确这些变量并采取小范围验证；当前按用户要求不再追加完整性能测试。
