# QuaRot 精度退化：EPLB 因果对照（进行中）

## 当前结论

**尚不能确认 EPLB 是根因。** 新版此前在 EPLB 更新附近出现持续感叹号，故障状态下单条请求也能复现；关闭 EPLB、延后重排的对照均未复现。但是恢复原重排周期后，也已观察到多次更新后回答正常。因此不能把时间相关性直接当作因果结论。

按用户要求，先完成因果确认，再定位具体实现问题。当前没有永久修复，也没有修复后的全量精度分数。

## 固定条件

- 三台服务器：192.168.13.197（Prefill TP16、代理）、192.168.13.198 和 192.168.13.159（Decode，合计 DP32/EP32）。
- 源码容器：三机均为 `qwen235-acc-20260919`。
- 基线容器：`qwen235-acc-v023-20260919`，镜像 `quay.io/ascend/vllm-ascend:v0.23.0-a3`。
- 权重：`/mnt/weight/Qwen3-235B-A22B-w8a8-QuaRot`。
- 新版：vLLM `2cf0a6915ce544dc493a0990f2ea38d81601128a`，Ascend `e139b7d573d3769fd1407d5027d7d4831f5469f0`。
- 基线：vLLM `0fc695fc6d1d82e9a5ac6835ac8e4e1c83703665`，Ascend `5cb98caaadeff42b5b62b996e34bb2aaa29d20fd`。
- 原始 prompt 不变；temperature=0.6，top_p=0.95，继承模型 top_k=20，max_tokens=32768，thinking 开启，ignore_eos=false。
- 小样本诊断使用流式返回，以便发现退化后立即停止；这是相对原 AISBench 请求的传输方式变化。正式全量验证仍须使用 AISBench 和 `--dump-eval-details`。
- 正常的 Wait、题干引用重复不作为失败。采样具有随机性，不要求逐 token 相同。

## 已完成对照

第一组固定样本为 GSM8K ID：12、93、98、100、106、119、120、125；并发为 8。

| 实验 | 结果 | 限制 |
| --- | --- | --- |
| v0.23.0 基线 | 8/8 正常结束，无同类退化 | 小样本结果，不是全量分数 |
| 新版原配置 | 第 100 条在约 166.86 秒出现连续 `!`，提前停止；保存 4 条正常结果和 1 条异常结果 | 其余在途请求被取消 |
| 新版故障状态下单条重放 | 第 12 条两次分别约 6.39、7.02 秒出现连续符号 | 第二次覆盖同名样本文件，首次数值来自运行输出 |
| 新版关闭动态 EPLB | 8/8 正常结束 | 该开关也改变部分量化执行路径，不能单独证明重排是原因 |
| 新版原配置加临时打点 | 8/8 正常结束；经历两轮更新 | 打点含同步操作，可能影响时序，不能作为修复证据 |
| 新版保持 EPLB 开启，仅延后触发 | 24/24 正常结束，约 643 秒，无同类退化 | 仅将两个 Decode 热度收集周期从 4000 改为 1000000000；没有发生重排 |
| 新版恢复原 4000 周期，无打点 | 24/24 正常结束，跨越多次更新，未复现 | 说明故障不是开启 EPLB 后必现 |
| 新版原周期、60 个历史 bad case、64 并发 | 按用户新指令停止，保存 162 条正常结束记录 | 计划 180 次，未完成，不是全量评分 |

延后触发组的完整原文审计只有第 119 条一次题干短语重复命中，属于基线也有的同类推理。没有连续字符、乱码或思考标签异常。

基线旧日志确认完成过 **12 次 EPLB 更新**，不是只设置了开关却从未执行。原配置初次复现、打点组、恢复组使用相同编译缓存目录键 `411168a4a0`；延后周期组为 `e55dcd606a`。

临时打点核对前两层的专家权重/scale 抽样值、映射内容和张量地址；全部 32 个 Decode rank 检查一致。这只覆盖前两层抽样，不能排除其他层、未采样元素或时序问题。三处临时源码修改已全部恢复为 e139 原文，后续对照没有打点。

## 后续判定

用户随后要求停止小样本诊断，直接按原方法关闭 EPLB 跑全量。`run05-noeplb-quarot-20260920` 已完成：使用原 AISBench GSM8K 1319 条、64 并发和 `--dump-eval-details`；只将两个 Decode 的 dynamic_eplb 改为 false，其他配置字节一致。没有永久代码改动。

结果 **1273/1319 = 96.512509%**。没有连续感叹号，但 1052、1078 两条出现持续算式/Hmm 循环，均达到 32768 token 上限，且在基线中正常答对。关闭 EPLB 后仍存在异常循环，说明 EPLB 开启不是所有重复的必要条件；原先连续感叹号与本轮短语循环不应未经验证就归为同一原因。详见 [全量结果和异常原文](../local-noeplb-quarot-20260920/README.md)。

## 证据

- [诊断实验的请求、回答、配置及日志摘录](evidence.tar.gz)：240 条保存记录，包含恢复 EPLB 的 24 条和按用户指令停止的压力实验 162 条；该压力实验仍不是计划 180 条的完整结果。
- 服务器原始目录：`/mnt/share/qwen235-acc-20260919/quarot-debug`，容器内为 `/workspace/quarot-debug`。
- 日志应从写入节点读取；共享文件系统跨机视图存在延迟。
- 198/159 的时钟约比 197 快 79–80 秒，不能直接按原始时间戳判断先后。

基线全量测试的保存结果为 1271/1314=96.727549%，不是完整 1319 条最终分数；详见 [统一汇总](../TEST_SUMMARY.md)。
