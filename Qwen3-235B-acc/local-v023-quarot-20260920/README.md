# v0.23.0 + QuaRot 精度复测（2026-09-20）

本轮推理服务使用 `quay.io/ascend/vllm-ascend:v0.23.0-a3`，模型为 `/mnt/weight/Qwen3-235B-A22B-w8a8-QuaRot`。三机拓扑、GSM8K 数据集和采样参数保持不变：temperature=0.6、top_p=0.95、模型默认 top_k=20、thinking=true、max_out_len=32768、batch_size=64；AISBench 实际命令包含 `--dump-eval-details`。

## 结果

客户端提交了全部 1,319 条请求，失败数为 0。AISBench 进度显示 1,318 条完成，但最后一条长请求长时间没有写入 JSONL；客户端随后停止，结果文件实际落盘 1,314 条。

按原 AISBench 后处理统计：

| 已落盘样本 | 正确 | 错误 | 局部精度 |
|---:|---:|---:|---:|
| 1314 | 1271 | 43 | 96.727549% |

这不是 1,319 条完整最终分数。若未落盘的 5 条全部错误/全部正确，理论边界约为 96.36%–96.80%；不能用边界代替最终分数。

## 输出审计

1,314 条回答中没有发现 U+FFFD `�`、长串重复字符、未闭合思考或完整回答重复。10 条命中“重复短语”启发式，人工检查均属于推理中重复题干或 `Wait`，最终回答有闭合 `</think>` 和数字答案；其中一个重复 `Wait` 较多，但没有上一轮源码环境出现的连续感叹号退化。

上一轮源码 v0.28.0 环境的 294 条快照为 81.97%，其中 60 条连续重复字符、56 条未闭合思考；本轮 v0.23.0 的 1,314 条落盘结果未复现该问题。两轮结果仍不是完全同一批完成时序，结论是现象对照，不是已证明的因果定位。

## 证据

- [部分原始结果归档](partial-evidence.tar.gz)
- 共享目录：`/mnt/share/qwen235-acc-20260919/run04-v023-quarot-20260920`
- JSONL：`client/outputs/default/20260920_075744/predictions/vllm-api-general-chat/tmp/`
- v0.23.0 服务容器名：`qwen235-acc-v023-20260919`；AISBench 客户端在 197 的已安装客户端容器运行。

测试结束时保留 v0.23.0 三机推理服务，客户端已停止以避免最后一条无进展请求继续占用资源。
